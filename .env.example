# ------------------ Environment Configuration ------------------
# Create a `.env` file at the root of the project and populate it
# with the following values for local development.
# You may also create a `.env.test` file for test-specific overrides.
#
# Note: These variables are used to configure Auth0 security integration.
# ----------------------------------------------------------------

# Enable or disable Auth0 security integration
AUTH0_ENABLED=true
# Auth0 tenant domain
# e.g. dev-4556nvnasd3masvna.auth0.com
AUTH0_DOMAIN=your‑tenant.auth0.com
# Auth0 Application credentials
# Go to Auth0 Dashboard → Applications → [Your App] → Settings
AUTH0_CLIENT_ID=YOUR_AUTH0_CLIENT_ID
AUTH0_CLIENT_SECRET=YOUR_AUTH0_CLIENT_SECRET
# Auth0 API Identifier
# Go to Auth0 Dashboard → APIs → [Your API] → Settings
AUTH0_AUDIENCE=https://insurance‑service
AUTH0_API_TOKEN=your-token-here